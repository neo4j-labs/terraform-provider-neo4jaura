---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "neo4jaura_organization_invites Data Source - neo4jaura"
subcategory: ""
description: |-
  Lists pending and historical invites for an Aura organization.
---

# neo4jaura_organization_invites (Data Source)

Lists pending and historical invites for an Aura organization.

-> **See also:** [Managing users in Aura](../guides/managing-users.md) for the end-to-end invite → import → manage flow.

## Example Usage

```terraform
data "neo4jaura_organization_invites" "this" {
  organization_id = var.organization_id
}

variable "organization_id" {
  type = string
}

output "pending_invites" {
  value = [
    for invite in data.neo4jaura_organization_invites.this.invites : invite
    if invite.status == "active"
  ]
}

# user_id is only populated for invites with status "accepted".
output "accepted_user_ids" {
  value = {
    for invite in data.neo4jaura_organization_invites.this.invites :
    invite.email => invite.user_id
    if invite.status == "accepted"
  }
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `organization_id` (String) The ID of the organization the invites belong to.

### Read-Only

- `id` (String) Placeholder identifier for the invites data source.
- `invites` (Attributes List) The list of invites for the organization. (see [below for nested schema](#nestedatt--invites))

<a id="nestedatt--invites"></a>
### Nested Schema for `invites`

Read-Only:

- `email` (String) The email address of the invitee.
- `expires_at` (String) The timestamp after which the invite can no longer be accepted.
- `id` (String) The unique identifier of the invite.
- `invited_by` (String) The ID of the user who created the invite.
- `organization_roles` (List of String) The organization roles granted to the invitee. Possible values: `organization-admin`, `organization-member`, `organization-owner`.
- `project_invites` (Attributes List) Project roles granted to the invitee alongside the organization role. (see [below for nested schema](#nestedatt--invites--project_invites))
- `status` (String) The status of the invite. Possible values: `active`, `accepted`, `revoked`, `expired`, `declined`.
- `user_id` (String) The ID of the user who accepted the invite. Populated once `status` is `accepted` by matching `email` against the organization's user list; `null` otherwise.

<a id="nestedatt--invites--project_invites"></a>
### Nested Schema for `invites.project_invites`

Read-Only:

- `project_id` (String) The ID of the project.
- `project_roles` (List of String) The project roles granted. Possible values: `project-admin`, `project-member`, `project-metrics-integration-reader`, `project-viewer`.
