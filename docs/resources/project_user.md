---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "neo4jaura_project_user Resource - neo4jaura"
subcategory: ""
description: |-
  Manages a user's membership in an Aura project.
---

# neo4jaura_project_user (Resource)

Manages a user's membership in an Aura project.

-> **See also:** [Managing users in Aura](../guides/managing-users.md) for the end-to-end invite → import → manage flow.

## Example Usage

```terraform
resource "neo4jaura_project_user" "this" {
  organization_id      = var.organization_id
  project_id           = var.project_id
  user_id              = var.user_id
  project_roles        = ["project-member"]
  deregister_on_delete = false
}

variable "organization_id" {
  type = string
}

variable "project_id" {
  type = string
}

variable "user_id" {
  type = string
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `organization_id` (String) The ID of the organization that contains the project.
- `project_id` (String) The ID of the project the user is being registered in.
- `user_id` (String) The ID of the user to register in the project.

### Optional

- `deregister_on_delete` (Boolean) When `true`, the user is removed from the project on resource deletion. When `false` (default), the user's membership is left intact and only the Terraform state is removed.
- `project_roles` (List of String) The roles assigned to the user within the project. Possible values: `project-admin`, `project-member`, `project-metrics-integration-reader`, `project-viewer`.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import neo4jaura_project_user.this <organization_id>,<project_id>,<user_id>
```
