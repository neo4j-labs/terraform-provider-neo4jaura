---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "neo4jaura_organization_invite Resource - neo4jaura"
subcategory: ""
description: |-
  Invites a user to an Aura organization by email, granting project roles at the same time.
  -> Note: The Aura API has no endpoint to edit a pending invite. Any change to email, organization_roles, or project_invites will revoke the existing invite and create a new one.
---

# neo4jaura_organization_invite (Resource)

Invites a user to an Aura organization by email, granting project roles at the same time.

-> **Note:** The Aura API has no endpoint to edit a pending invite. Any change to `email`, `organization_roles`, or `project_invites` will revoke the existing invite and create a new one.

-> **See also:** [Managing users in Aura](../guides/managing-users.md) for the end-to-end invite → import → manage flow.

## Example Usage

```terraform
resource "neo4jaura_organization_invite" "this" {
  organization_id    = var.organization_id
  email              = "new.teammate@example.com"
  organization_roles = ["organization-member"]

  project_invites = [
    {
      project_id    = var.project_id
      project_roles = ["namespace-member"]
    }
  ]
}

variable "organization_id" {
  type = string
}

variable "project_id" {
  type = string
}

output "invite_status" {
  value = neo4jaura_organization_invite.this.status
}

# Populated once the invite is accepted — see the "Managing users in Aura" guide.
output "accepted_user_id" {
  value = neo4jaura_organization_invite.this.user_id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `email` (String) The email address of the invitee.
- `organization_id` (String) The ID of the organization to invite the user to.
- `organization_roles` (Set of String) The organization roles to grant the invitee. Possible values: `organization-admin`, `organization-member`, `organization-owner`.
- `project_invites` (Attributes List) Project roles to grant the invitee at the same time, alongside the organization role. The Aura API requires at least one entry — every invite must grant access to at least one project. (see [below for nested schema](#nestedatt--project_invites))

### Read-Only

- `expires_at` (String) The timestamp after which the invite can no longer be accepted.
- `id` (String) The unique identifier of the invite.
- `invited_by` (String) The ID of the user who created the invite.
- `status` (String) The status of the invite. Possible values: `active`, `accepted`, `revoked`, `expired`, `declined`.
- `user_id` (String) The ID of the user who accepted the invite. Populated once `status` becomes `accepted` by matching `email` against the organization's user list; `null` otherwise.

<a id="nestedatt--project_invites"></a>
### Nested Schema for `project_invites`

Required:

- `project_id` (String) The ID of the project to grant roles in.
- `project_roles` (Set of String) The project roles to grant. Possible values: `namespace-admin`, `namespace-member`, `namespace-metrics-integration-reader`, `namespace-viewer`.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import neo4jaura_organization_invite.this <organization_id>,<invite_id>
```
